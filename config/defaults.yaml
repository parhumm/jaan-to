# jaan.to Plugin - Default Configuration
# Version: 3.0.0
# DO NOT EDIT - These are plugin defaults
# Customize via jaan-to/config/settings.yaml in your project

version: "3.0"

# Paths (flattened for bash parsing)
paths_templates: "jaan-to/templates"
paths_learning: "jaan-to/learn"
paths_context: "jaan-to/context"
paths_outputs: "jaan-to/outputs"
paths_docs: "jaan-to/docs"

# Template settings
templates_inheritance_enabled: true

# Learning settings
learning_strategy: "merge"
learning_merge_order: "plugin,project"

# Customization features
customization_path_variables: true
customization_template_variables: true
customization_context_variables: true

# Language preference for conversation and output
# "ask" = prompt on first skill run, then save choice
language: "ask"

# Issue report submission preference
# "ask" = prompt on first run if gh CLI is available, then save choice
issue_report_submit: "ask"

# Integration drift detection
# When true, PostToolUse hook warns about un-integrated outputs
integration_drift_check: true

# Agent Teams (experimental)
# Requires CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 in environment
# Enable via project settings.yaml: agent_teams_enabled: true
agent_teams_enabled: false
agent_teams_default_track: "full"     # Default track: fast, full
agent_teams_display_mode: "auto"      # Display: auto, in-process, split-pane
agent_teams_plan_approval: true       # Require user approval before spawning
agent_teams_quality_gate: true        # Check outputs on task completion
agent_teams_teammate_model: ""        # Override teammate model (empty = per-role)
agent_teams_detect_model: "haiku"     # Model for detect-* auditor roles
